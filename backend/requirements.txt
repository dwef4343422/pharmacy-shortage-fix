# =============================================
# Pharmacy Smart Shortage Manager — Backend
# Python Dependencies
# =============================================

# Core Framework
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
pydantic==2.7.4
pydantic-settings==2.3.4

# Database
sqlalchemy[asyncio]==2.0.31
asyncpg==0.29.0
alembic==1.13.1
greenlet==3.0.3

# OCR & Image Processing
# NOTE: PaddleOCR (paddleocr/paddlepaddle) is intentionally omitted. It's a ~400MB
# dependency with no wheels for newer Python, and the deployed app uses the Tesseract
# engine (OCR_ENGINE=tesseract). Install it manually (`pip install paddleocr paddlepaddle`)
# only for higher-accuracy OCR in a local/self-hosted setup.
pytesseract==0.3.10
Pillow==10.3.0
opencv-python-headless==4.10.0.84
numpy==1.26.4

# File Parsing
openpyxl==3.1.5
pandas==2.2.2
pdfplumber==0.11.1

# Export Generation
reportlab==4.2.2
xlsxwriter==3.2.0

# Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.3

# Utilities
python-dotenv==1.0.1
aiofiles==24.1.0
httpx==0.27.0
rapidfuzz==3.9.4

# Testing
pytest==8.2.2
pytest-asyncio==0.23.7
